<h1>Listado de Comentarios del Post de titulo <%= @post.title %></h1>
<table>
  <thead>
    <th>Contenido<th>
  </thead>
  <tbody>
<% @comments.each do |comment| %>
  <tr>
    <td><%= comment.content %></td>
  </tr>
<% end %>
  </tbody>
</table>
